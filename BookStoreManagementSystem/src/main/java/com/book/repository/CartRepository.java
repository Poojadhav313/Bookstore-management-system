package com.book.repository;

import org.springframework.stereotype.Repository;

@Repository
public class CartRepository {
    // This can be handled in service layer as Cart is not stored in the database.
}